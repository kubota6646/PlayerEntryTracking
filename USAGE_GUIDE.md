# 使い方ガイド - PlayerEntryTracking

## 目次
1. [概要](#概要)
2. [インストール手順](#インストール手順)
3. [設定方法](#設定方法)
4. [動作確認](#動作確認)
5. [トラブルシューティング](#トラブルシューティング)
6. [よくある質問](#よくある質問)

## 概要

PlayerEntryTrackingは、BungeeCordプロキシサーバー用のシンプルなプラグインです。
プレイヤーの行動（参加、サーバー移動、退出）を自動的に全員に通知します。

## インストール手順

### 1. プラグインのビルド

プロジェクトをクローンしてビルドします：

```bash
git clone https://github.com/kubota6646/PlayerEntryTracking.git
cd PlayerEntryTracking
./gradlew build
```

ビルドが成功すると、`build/libs/PlayerEntryTracking-1.1.0.jar`が生成されます。

### 2. プラグインのインストール

1. BungeeCordサーバーのインストールディレクトリを開きます
2. `plugins`フォルダを開きます（なければ作成します）
3. `PlayerEntryTracking-1.1.0.jar`を`plugins`フォルダにコピーします

### 3. サーバーの起動

BungeeCordサーバーを起動または再起動します：

```bash
java -jar BungeeCord.jar
```

コンソールに以下のようなメッセージが表示されれば成功です：
```
[INFO] PlayerEntryTracking プラグインが有効になりました
[INFO] config.ymlを読み込みました
[INFO] イベントリスナーの登録が完了しました
```

初回起動時に `plugins/PlayerEntryTracking/config.yml` が自動生成されます。

## 設定方法

### 設定ファイルの場所

`plugins/PlayerEntryTracking/config.yml`

### メッセージのカスタマイズ

config.ymlを編集してメッセージをカスタマイズできます：

```yaml
messages:
  join: "&e{player}さんが参加しました"
  switch: "&e{player}さんが{server}サーバーへ移動しました"
  quit: "&e{player}さんが退出しました"
```

#### 使用可能なプレースホルダー

- `{player}` - プレイヤー名
- `{server}` - サーバー名（switchメッセージのみ使用可能）

#### カラーコード

| コード | 効果 | コード | 効果 |
|--------|------|--------|------|
| `&0` | 黒 | `&8` | 暗い灰色 |
| `&1` | 濃い青 | `&9` | 青 |
| `&2` | 濃い緑 | `&a` | 緑 |
| `&3` | 濃い水色 | `&b` | 水色 |
| `&4` | 濃い赤 | `&c` | 赤 |
| `&5` | 濃い紫 | `&d` | 紫 |
| `&6` | 金色 | `&e` | 黄色 |
| `&7` | 灰色 | `&f` | 白 |
| `&l` | 太字 | `&m` | 取り消し線 |
| `&n` | 下線 | `&o` | 斜体 |
| `&r` | リセット | | |

#### カスタマイズ例

```yaml
# シンプルなスタイル
messages:
  join: "&a[+] &f{player}"
  switch: "&b{player} &7→ &e{server}"
  quit: "&c[-] &f{player}"

# 詳細なスタイル
messages:
  join: "&a&l参加 &r&7| &f{player} &7がサーバーに参加しました"
  switch: "&b&l移動 &r&7| &f{player} &7が &e{server} &7に移動しました"
  quit: "&c&l退出 &r&7| &f{player} &7がサーバーから退出しました"
```

### 通知の有効/無効

特定の通知を無効にできます：

```yaml
notifications:
  join: true    # 参加通知を表示
  switch: false # サーバー移動通知を非表示
  quit: true    # 退出通知を表示
```

### 設定の再読み込み

設定ファイルを編集した後、BungeeCordサーバーを再起動して変更を反映します。

## 動作確認

### プレイヤー参加の確認

1. Minecraftクライアントを起動します
2. BungeeCordサーバーに接続します
3. 接続すると、すべてのプレイヤーに以下のメッセージが表示されます：
   ```
   [プレイヤー名]さんが参加しました
   ```

### サーバー移動の確認

1. BungeeCordプロキシに複数のサーバーを登録しておきます
2. `/server [サーバー名]`コマンドでサーバーを移動します
3. 移動すると、すべてのプレイヤーに以下のメッセージが表示されます：
   ```
   [プレイヤー名]さんが[サーバー名]サーバーへ移動しました
   ```

### プレイヤー退出の確認

1. Minecraftクライアントを終了します
2. すべてのプレイヤーに以下のメッセージが表示されます：
   ```
   [プレイヤー名]さんが退出しました
   ```

## トラブルシューティング

### プラグインが読み込まれない

**症状**: サーバー起動時にプラグインのメッセージが表示されない

**解決方法**:
1. JARファイルが`plugins`フォルダに正しく配置されているか確認
2. ファイル名が正しいか確認（`PlayerEntryTracking-1.1.0.jar`）
3. BungeeCordのバージョンが1.19以降であることを確認
4. サーバーのログファイルでエラーメッセージを確認

### メッセージが表示されない

**症状**: プラグインは読み込まれるがメッセージが表示されない

**解決方法**:
1. BungeeCordのログファイルでエラーがないか確認
2. プラグインが正しく有効化されているか`/glist`コマンドで確認
3. `config.yml`で該当の通知が有効になっているか確認（`notifications`セクション）
4. 他のプラグインとの競合がないか確認

### カラーコードが正しく表示されない

**症状**: メッセージに`&e`などの文字がそのまま表示される

**解決方法**:
1. config.ymlの記述が正しいか確認（YAMLの文法エラーがないか）
2. 文字列をダブルクォート（`"`）で囲む必要がある場合があります：
   ```yaml
   messages:
     join: "&e{player}さんが参加しました"
   ```

### 設定ファイルが生成されない

**症状**: `plugins/PlayerEntryTracking/config.yml`が作成されない

**解決方法**:
1. BungeeCordサーバーを再起動して、プラグインを完全に読み込む
2. `plugins`フォルダの書き込み権限を確認
3. サーバーログでエラーメッセージを確認

### ビルドエラーが発生する

**症状**: `./gradlew build`でエラーが発生する

**解決方法**:
1. Java 17以降がインストールされているか確認: `java -version`
2. Gradleキャッシュをクリア: `./gradlew clean`
3. 依存関係を再ダウンロード: `./gradlew build --refresh-dependencies`

## よくある質問

### Q1: Spigot/Paperサーバーでも使えますか？

A: いいえ、このプラグインはBungeeCord専用です。Spigot/Paperサーバー単体では動作しません。BungeeCordプロキシサーバーにインストールする必要があります。

### Q2: メッセージの内容をカスタマイズできますか？

A: はい、バージョン1.1.0からconfig.ymlでメッセージをカスタマイズできます。`plugins/PlayerEntryTracking/config.yml`を編集して、メッセージや色を自由に変更できます。

### Q3: 特定のプレイヤーだけに通知を表示しないようにできますか？

A: 現在のバージョンではすべてのプレイヤーに通知が表示されます。ただし、config.ymlで通知の種類ごとに有効/無効を切り替えることができます。権限による個別制御は将来のバージョンで検討します。

### Q4: 他のMinecraftバージョンでも動作しますか？

A: このプラグインはMinecraft 1.19.4向けに開発されていますが、BungeeCord API 1.20-R0.2を使用しているため、1.19.x～1.20.xのバージョンで動作するはずです。

### Q5: Waterfallでも動作しますか？

A: はい、WaterfallはBungeeCordのフォーク版なので、このプラグインは問題なく動作します。

### Q6: メッセージの色を変更できますか？

A: はい、バージョン1.1.0からconfig.ymlで色を変更できます。`&a`（緑）、`&c`（赤）、`&b`（水色）などのカラーコードを使用して、自由にカスタマイズできます。

### Q7: プレースホルダーを追加できますか？

A: 現在サポートされているプレースホルダーは`{player}`（プレイヤー名）と`{server}`（サーバー名）のみです。将来のバージョンで追加のプレースホルダーを検討します。

## サポート

問題が解決しない場合や、機能の要望がある場合は、GitHubのIssuesセクションで報告してください。

プロジェクトURL: https://github.com/kubota6646/PlayerEntryTracking
